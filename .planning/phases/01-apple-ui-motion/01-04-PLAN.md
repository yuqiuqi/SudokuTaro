---
phase: 01-apple-ui-motion
plan: "04"
type: execute
wave: 1
depends_on:
  - "01-03"
files_modified:
  - src/app.scss
  - src/pages/index/index.scss
  - .planning/phases/01-apple-ui-motion/01-VALIDATION.md
  - .planning/phases/01-apple-ui-motion/01-RESEARCH.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - EXP-02
  - EXP-05
  - TEC-01
user_setup: []
must_haves:
  truths:
    - 不删除或重命名 `src/styles/_apple-ui-tokens.scss` 的既有真源；仅在其或 app/index 中做**增量**。
    - 不恢复「操作行上默认长 transition」以免破坏现 `hoverStayTime={0}` / 无粘滞策略。
  artifacts:
    - 01-REPLAN-NOTE.md（与 01-REVIEWS 对齐的基线说明）
  key_links:
    - 与 `01-REVIEWS.md` Consensus 一致：blur 降级、真机/工具验证、token 不重复
---

<objective>
在 **不推翻已合并的 Phase 1 实现** 的前提下，吸收 `01-REVIEWS.md`（含 Codex 的 re-baseline 建议、Gemini 的 H5/毛玻璃建议）：补齐 **文档真源链**、**无 `backdrop-filter` 时的可访问实色**、**全局 H5 点击高亮**、**验证表中的微信端检查项**；可选对照代码复核 REQUIREMENTS 勾选。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-apple-ui-motion/01-REVIEWS.md
@.planning/phases/01-apple-ui-motion/01-REPLAN-NOTE.md
@.planning/phases/01-apple-ui-motion/01-UI-SPEC.md
@src/styles/_apple-ui-tokens.scss
@src/app.scss
@src/pages/index/index.scss
</context>

<threat_model>
| threat_id | category | component | mitigation |
|-----------|----------|-----------|------------|
| TM-UI-01 | 展示层 | 仅样式与文档 | 无新网络面 |

</threat_model>

<tasks>

<task type="auto">
  <name>全局 H5 去灰框（tap highlight）</name>
  <files>src/app.scss</files>
  <read_first>src/app.scss,src/pages/index/index.scss</read_first>
  <action>在 `app.scss` 的 `page` 与 `body` 规则中增加 **`-webkit-tap-highlight-color: transparent;`**（与现有 `index.scss` 中 `.actions .btn--action` 上已有规则互补：此处为全页主路径，减少 H5 灰框）。不删除按钮级 `tap-highlight`。</action>
  <verify>grep -n</verify>
  <acceptance_criteria>
    - `src/app.scss` 中 `page` 或 `body` 的代码块内出现 `-webkit-tap-highlight-color: transparent`
  </acceptance_criteria>
  <done>H5 主路径与评审建议对齐</done>
</task>

<task type="auto">
  <name>无 backdrop 支持时的实色回退</name>
  <files>src/pages/index/index.scss,src/styles/_apple-ui-tokens.scss</files>
  <read_first>src/pages/index/index.scss,src/styles/_apple-ui-tokens.scss</read_first>
  <action>使用 CSS **`@supports not** ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px)))` **{ … }` 为 `.content-wrap` 与 `.modal__card` 提供略高的不透明白/浅灰实底（可引用已有 `$ui-surface` / `$ui-glass-*`），保证在**不支持毛玻璃**的环境下仍可读；勿删除原有带 blur 的规则。</action>
  <verify>npm run build:h5 无错</verify>
  <acceptance_criteria>
    - `index.scss` 含 `@supports not` 且选择器同时覆盖或分别覆盖 `.content-wrap` 与 `.modal__card` 之一
    - 该块内 `background` 或 `background-color` 非透明纯文字（至少一处使用变量或 #fff 系实色）
  </acceptance_criteria>
  <done>与 Gemini 建议的「blur 不可用时仍 legible」一致</done>
</task>

<task type="auto">
  <name>01-VALIDATION 增补微信/性能行</name>
  <files>.planning/phases/01-apple-ui-motion/01-VALIDATION.md</files>
  <read_first>.planning/phases/01-apple-ui-motion/01-VALIDATION.md</read_first>
  <action>在「Manual-Only」或同等级表格中**新增**至少两行：**（1）** 微信开发者工具 + 一台真机，打开主路径，主观观察**毛玻璃/模糊**是否卡顿或无效；**（2）** 在弱机或关闭 GPU 加速场景下，确认与 `@supports` 回退不冲突。可简要引用 `01-REVIEWS` 共识。</action>
  <verify>read file</verify>
  <acceptance_criteria>
    - 文件中同时出现 `微信` 与 `真机` 或 `实机` 关键词
    - 文件中 `backdrop` 或 `毛玻璃` 或 `blur` 至少出现一次
  </acceptance_criteria>
  <done>评审中「工具链验证」可追踪</done>
</task>

<task type="auto">
  <name>RESEARCH 增补 Token 真源链</name>
  <files>.planning/phases/01-apple-ui-motion/01-RESEARCH.md</files>
  <read_first>.planning/phases/01-apple-ui-motion/01-RESEARCH.md</read_first>
  <action>在文件末尾或「关键文件」表附近增加固定小节 **「Token 真源（评审后）」**，用项目内相对链接列出：`src/styles/_apple-ui-tokens.scss` → `src/pages/index/_ui-tokens.scss` @forward → `src/utils/theme.ts` → `minigame-wechat/src/appleUiTheme.ts`（手同步说明一句）。不修改代码。</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - `01-RESEARCH.md` 含子串 `_apple-ui-tokens` 与 `appleUiTheme`
  </acceptance_criteria>
  <done>Codex「单一事实来源」文档化</done>
</task>

<task type="auto">
  <name>REQUIREMENTS 对照复核（只读+勾选，不改 EXP 含义）</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>在 **Traceability** 或各 EXP 下增加备注行或更新 Status：对已在仓库可验证的项（如 token 全量、reduced-motion、主路径无 width 动画等）标为 `Done (review 2026-04-23)` 或仍 `Pending` 并**一句**说明缺何证据（UAT/设备）。不删除原有 ID。</action>
  <verify>人工</verify>
  <acceptance_criteria>
    - `REQUIREMENTS.md` 含字符串 `2026-04-23` 或 `review`
  </acceptance_criteria>
  <done>与评审「grep 通过≠视觉完成」形成对照</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build:h5` 无 error
- [ ] `npm run build:weapp` 无 error（若环境可跑）
</verification>

<success_criteria>
- 01-REVIEWS 中「可执行、不机械重跑旧 PLAN」的项已落到**代码或验证表/研究文档**中至少各一类。
</success_criteria>

<output>
完成后创建 `01-04-SUMMARY.md`（简述变更文件与未决项，若有）。
</output>

## PLANNING COMPLETE
