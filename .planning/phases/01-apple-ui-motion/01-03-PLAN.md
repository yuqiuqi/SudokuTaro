---
phase: 01-apple-ui-motion
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/index/index.scss
  - src/pages/index/index.tsx
autonomous: true
requirements:
  - EXP-02
  - EXP-04
  - EXP-05
  - TEC-01
user_setup: []
must_haves:
  truths:
    - `prefers-reduced-motion` 媒体查询存在于 index.scss
    - 模态可见态使用 transform/opacity 过渡类
    - Hero 漂移动画在 reduced-motion 下禁用或暂停
  artifacts:
    - src/pages/index/index.scss
    - src/pages/index/index.tsx
  key_links:
    - 过渡时长使用 `$ui-dur-tap` 等 token
---

<objective>
落地 **动效契约**（按下态、弹层进出场、冲突反馈辅助）与 **`prefers-reduced-motion`**；确保主路径动画以 transform/opacity 为主（EXP-05）；收口 TEC-01（样式层仅引用 token）。
</objective>

<execution_context>
@C:\Users\yuqiu\.cursor\get-shit-done\workflows\execute-plan.md
@C:\Users\yuqiu\.cursor\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-apple-ui-motion/01-UI-SPEC.md
@src/pages/index/index.tsx
@src/pages/index/index.scss
@CONTEXT.md
</context>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-UI-01 | Tampering | 客户端 | Low | 无新攻击面 |

</threat_model>

<tasks>

<task type="auto">
  <name>按钮与格子的按下态（transform）</name>
  <files>src/pages/index/index.scss,src/pages/index/index.tsx</files>
  <read_first>src/pages/index/index.tsx,src/pages/index/index.scss,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>为可点击 **操作按钮**（含 `btn--action` 等）添加 `transition: transform $ui-dur-tap $ui-ease-standard`，`:active` 使用 `transform: scale(0.97)`。若小程序对 `:active` 支持弱，保留现有 `hoverClass` 并 **增加** 对等的 `active` 类样式。对 **可选中格子**（`.grid__cell` 或实际类名以代码为准）：选中态已有则增强 **200ms** 焦点过渡。禁止对 `width`/`height` 做 transition。</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - `index.scss` 含 `scale(0.97)` 或 `scale(0.98)`
    - `index.scss` 含 `$ui-ease-standard` 或 `cubic-bezier(0.25, 0.1, 0.25, 1)`
  </acceptance_criteria>
  <done>EXP-02 按下反馈</done>
</task>

<task type="auto">
  <name>模态进出场类 + TSX 挂载</name>
  <files>src/pages/index/index.scss,src/pages/index/index.tsx</files>
  <read_first>src/pages/index/index.tsx,src/pages/index/index.scss</read_first>
  <action>因模态为 **条件渲染**（`wonOpen ? … : null`），挂载即视为打开。任选其一实现 **入场**（须仅用 opacity/transform）：**(A)** 为 `.modal__card` 定义 `@keyframes`（如 `modalSheetIn`）`from { opacity:0; transform: translateY(24rpx) scale(0.98); }` `to { opacity:1; transform: none; }`，`animation: modalSheetIn $ui-dur-sheet-enter $ui-ease-sheet forwards`；或 **(B)** 根 `View` 增加 `modal--open` 类并用 `transition`（若用 B，需在 TSX 保证首帧可过渡）。Mask 可用 `animation` 淡入。关闭为卸载则无离场动画，可接受。**三处** 模态（胜利/设置/帮助）行为一致。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.scss` 含 `modalSheetIn` 或 `modal--open` 之一
    - `index.scss` 中 `.modal__card` 相关动效仅使用 `opacity` / `transform`（无 `width`/`height` transition）
  </acceptance_criteria>
  <done>EXP-04 弹层动效</done>
</task>

<task type="auto">
  <name>prefers-reduced-motion 与 Hero 动画降级</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>在 `index.scss` 末尾添加 `@media (prefers-reduced-motion: reduce) { ... }`：**所有** `heroDrift*` `animation` 设为 `none`；`.modal__card` 过渡改为 `opacity` only 且 `max-duration: 120ms`；按钮 `transform` 过渡禁用或缩至 ≤120ms。全局可选 `* { animation-duration: 0.01ms !important; }` **不要**使用（过于粗暴）；仅针对已列选择器。</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - `index.scss` 含 `prefers-reduced-motion`
    - 该媒体块内含 `animation` 与 `none`（针对 hero）或 `hero__` 选择器
  </acceptance_criteria>
  <done>UI-SPEC Motion 降级</done>
</task>

<task type="auto">
  <name>冲突格过渡（可选轻量）</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss</read_first>
  <action>为 `.grid__cell` 冲突类名（以现有 `conflict` / `is-conflict` 为准，先 read 文件确认）增加 **180–240ms** 的 `background-color` 或 `box-shadow` 过渡（优先不改变布局）。若当前用纯 class 切换无过渡，添加与 `$ui-conflict-tint` 相关过渡。</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - 冲突相关选择器含 `transition` 且 duration 在 `0.18s`–`0.24s` 或 `180ms`–`240ms` 区间（任一处）
  </acceptance_criteria>
  <done>冲突视觉有短过渡</done>
</task>

<task type="auto">
  <name>TEC-01 收口审计（grep）</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/_ui-tokens.scss</read_first>
  <action>在 `index.scss` 中，将仍残留的 **主色域** 裸 hex（`#1C1C1E`, `#007AFF`, `#F5F2EB` 等）尽量替换为 `$ui-*` 变量；**允许**渐变中保留额外强调色若与 UI-SPEC hero 光斑一致，但须在注释标明 `/* hero accent — not semantic token */`。完成自检清单：至少 **10 处** 非 token 颜色已迁移或已注释豁免。</action>
  <verify>人工 + grep 计数</verify>
  <acceptance_criteria>
    - `_ui-tokens.scss` 仍被 index 引用
    - `index.scss` 中 `$ui-` 出现次数 ≥ 15（迁移为主，未强制全量）
  </acceptance_criteria>
  <done>TEC-01 单一来源收口</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build:h5` 与 `npm run build:weapp` 无 error
- [ ] 浏览器：Toggle reduced-motion，确认 hero 与 modal 行为
</verification>

<success_criteria>
- EXP-02/04/05 与 TEC-01 在本 Plan 闭环
</success_criteria>

<output>
完成后创建 `01-03-SUMMARY.md`
</output>

## PLANNING COMPLETE
