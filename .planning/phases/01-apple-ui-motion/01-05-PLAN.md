---
phase: 01-apple-ui-motion
plan: "05"
type: execute
wave: 2
depends_on:
  - "01-04"
files_modified:
  - src/app.scss
  - src/pages/index/index.scss
  - .planning/phases/01-apple-ui-motion/01-04-SUMMARY.md
  - .planning/phases/01-apple-ui-motion/01-VALIDATION.md
  - .planning/phases/01-apple-ui-motion/01-RESEARCH.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - EXP-02
  - EXP-05
  - TEC-01
  - EXP-01
  - EXP-03
  - EXP-04
user_setup: []
must_haves:
  truths:
    - 不删改 `01-01`～`01-04` 既有 PLAN 文件；本 plan 为**增补**。
    - 代码增量仍只动 `app.scss` / `index.scss` 等与 `01-04` 一致的路径，不推翻 `_apple-ui-tokens.scss` 真源结构。
  artifacts:
    - `01-04-SUMMARY.md`（本波完成后必须存在）
  key_links:
    - `01-UI-SPEC.md`（2026-04-23）中 H5 tap-highlight 与 `@supports not` 毛玻璃回退
    - `01-04-PLAN.md` 任务验收
---

<objective>
在 **`01-04-PLAN` 可执行项尚未全部在仓库中可 grep 证明** 的前提下，**补齐**全局 tap-highlight、**无 backdrop** 时主容器实色回退、补充文档/traceability 与 **`01-04-SUMMARY.md`**，并跑通静态构建验证，为 UAT/transition 收口。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-apple-ui-motion/01-04-PLAN.md
@.planning/phases/01-apple-ui-motion/01-REPLAN-NOTE.md
@.planning/phases/01-apple-ui-motion/01-UI-SPEC.md
@.planning/phases/01-apple-ui-motion/01-UAT.md
@src/app.scss
@src/pages/index/index.scss
@src/styles/_apple-ui-tokens.scss
</context>

<threat_model>
| threat_id | category | component | mitigation |
|-----------|----------|-----------|------------|
| TM-UI-01 | 展示层 | 仅样式/文档 | 无新网络面 |
</threat_model>

<tasks>

<task type="auto">
  <name>全页 H5 tap-highlight（与 01-04 AC 对齐）</name>
  <files>src/app.scss</files>
  <read_first>src/app.scss,.planning/phases/01-apple-ui-motion/01-04-PLAN.md</read_first>
  <action>在 `page` 与 `body` 选择器内各增加 **`-webkit-tap-highlight-color: transparent;`**（若已存在则跳过，仅验收）。不删除子组件上已有 `tap-highlight` 规则。</action>
  <verify>grep -n -E 'page|body' src/app.scss; rg "tap-highlight" src/app.scss</verify>
  <acceptance_criteria>
    - `src/app.scss` 中同时满足：`page` 块含 `-webkit-tap-highlight-color: transparent`；`body` 块含同一属性（可在两行或同规则扩展）
  </acceptance_criteria>
  <done>满足 `01-04` 与 `01-UI-SPEC` 全局 H5 契约</done>
</task>

<task type="auto">
  <name>无 backdrop 时 `.content-wrap` / `.modal__card` 实色回退</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss,src/styles/_apple-ui-tokens.scss,.planning/phases/01-apple-ui-motion/01-04-PLAN.md</read_first>
  <action>若不存在，在 `index.scss` 增加 **`@supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) { ... }`** 块，对 **`.content-wrap`** 与 **`.modal__card`（或等效主弹层类）** 使用 `$ui-surface` / `$ui-glass-fill-strong` 等**不透明或近不透明**背景，使文字可读；**不得**删除原有 `backdrop-filter` 规则。选择器可分两处若更易维护，但 `@supports` 仅一份即可。</action>
  <verify>rg "@supports not" src/pages/index/index.scss; npm run build:h5</verify>
  <acceptance_criteria>
    - `index.scss` 含子串 `@supports not` 与 `backdrop-filter: blur(1px)`
    - 上述 `@supports` 块内出现 `.content-wrap` 与包含 `modal` 的类名（如 `.modal__card`）之一即通过（可分两规则）
  </acceptance_criteria>
  <done>与 01-04 / 评审「blur 不可用时仍 legible」一致</done>
</task>

<task type="auto">
  <name>01-04 文档行（仅当 01-04 未落时补）</name>
  <files>.planning/phases/01-apple-ui-motion/01-VALIDATION.md,.planning/phases/01-apple-ui-motion/01-RESEARCH.md,.planning/REQUIREMENTS.md</files>
  <read_first>.planning/phases/01-apple-ui-motion/01-VALIDATION.md,.planning/phases/01-apple-ui-motion/01-RESEARCH.md,.planning/REQUIREMENTS.md</read_first>
  <action>对照 **`01-04-PLAN.md`** 中「01-VALIDATION 增补微信」「RESEARCH Token 真源链」「REQUIREMENTS 对照复核」三节：若文件已含对应关键词（`微信`+`真机`、`_apple-ui-tokens`+`appleUiTheme`、`2026-04-23` 或 `review`），则本任务记为 **noop** 并在 `01-04-SUMMARY` 中注明已满足；否则按 `01-04` 原文补写最小增量。</action>
  <verify>grep 关键词</verify>
  <acceptance_criteria>
    - 若 `01-RESEARCH.md` 不含 `_apple-ui-tokens` 或 `appleUiTheme` 中任一，则补到至少同时出现两子串
    - 若 `REQUIREMENTS.md` 无 `2026-04-23` 与 `review` 同现，则按 `01-04` 加一行日期/审阅备注
  </acceptance_criteria>
  <done>文档与 `01-04` 验收链闭合</done>
</task>

<task type="auto">
  <name>撰写 01-04-SUMMARY.md</name>
  <files>.planning/phases/01-apple-ui-motion/01-04-SUMMARY.md</files>
  <read_first>.planning/phases/01-apple-ui-motion/01-01-SUMMARY.md</read_first>
  <action>新建 **`01-04-SUMMARY.md`**：列出本 milestone 在 `01-04`+`01-05` 中**实际改动的文件路径**、完成的 grep 条件、未决项（若有：如 weapp 构建环境缺失）；1 页内，中文。</action>
  <verify>test -f</verify>
  <acceptance_criteria>
    - 路径 `.planning/phases/01-apple-ui-motion/01-04-SUMMARY.md` 存在
    - 文件含标题或首段含 `01-04` 或 `补遗`
    - 正文中出现至少 2 个以 `src/` 或 `.planning/` 开头的路径片段
  </acceptance_criteria>
  <done>供 `/gsd-next` 与 transition 引用的结项摘要</done>
</task>

<task type="auto">
  <name>静态验证与 STATE 提示行</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md</read_first>
  <action>在仓库根执行 **`npx tsc --noEmit`** 与 **`npm run build:h5`**，二者 exit 0。若 `npm run build:weapp` 可跑则加跑一次并记入 `01-04-SUMMARY`。更新 **`.planning/STATE.md`** 的 `Last action` 一行，写「`/gsd-plan-phase 1` 增补 01-05-PLAN 已落地」与指向 `01-04-SUMMARY.md`。</action>
  <verify>shell</verify>
  <acceptance_criteria>
    - 终端中 `npx tsc --noEmit` 退出码 0
    - 终端中 `npm run build:h5` 退出码 0
    - `STATE.md` 含子串 `01-05-PLAN` 或 `01-04-SUMMARY`
  </acceptance_criteria>
  <done>执行波可收工/交 UAT</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build:h5` 无 error
- [ ] （可选）`npm run build:weapp` 无 error
</verification>

<success_criteria>
- 01-04 在代码与文档上的「可 grep」验收项在仓库中为真，或在本 plan 的 task 中显式补齐。
- 存在 `01-04-SUMMARY.md` 与更新后的 `STATE.md` 痕迹。
</success_criteria>

## PLANNING COMPLETE
