---
phase: 01-apple-ui-motion
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/pages/index/index.scss
  - src/app.scss
autonomous: true
requirements:
  - EXP-01
  - EXP-03
user_setup: []
must_haves:
  truths:
    - `.stats` 或计时步数字号 ≥ 26rpx（grep `font-size` 数值）
    - `.modal__title` / body 使用 token 色与圆角语义
    - 主 CTA「再来一局」相关按钮使用 `$ui-accent` 或基于 accent 的渐变
  artifacts:
    - src/pages/index/index.scss
  key_links:
    - 模态与按钮颜色不再使用裸 `#292524` 作为主 CTA（可保留 ghost 边框但须用 token）
---

<objective>
将棋盘外围 **表面层级、排版阶梯、模态与按钮** 对齐 `01-UI-SPEC.md`；满足 EXP-01（层级清晰）与 EXP-03（字号阶梯）。
</objective>

<execution_context>
@C:\Users\yuqiu\.cursor\get-shit-done\workflows\execute-plan.md
@C:\Users\yuqiu\.cursor\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-apple-ui-motion/01-UI-SPEC.md
@src/pages/index/index.scss
@src/app.scss
@src/pages/index/index.tsx
</context>

<threat_model>
**Trust boundaries:** 同 Plan 01；仅样式。

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-UI-01 | Tampering | 样式 | Low | 无 |

</threat_model>

<tasks>

<task type="auto">
  <name>全局字体栈（可选但推荐）</name>
  <files>src/app.scss,src/pages/index/index.scss</files>
  <read_first>src/app.scss,src/pages/index/index.scss,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>若 `app.scss` 或根存在 `font-family`，则合并为 UI-SPEC 所列栈：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", Roboto, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`。若项目仅在 `.page` 设字体，则将 **同一字符串** 写入 `.page` 的 `font-family`，并删除冲突的旧 `ui-sans-serif` 优先序 **或** 把 `ui-sans-serif` 保留在 `-apple-system` 之前（二选一以统一为准）。最终全站主路径应出现 `BlinkMacSystemFont`。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.scss` 或 `app.scss` 之一含 `BlinkMacSystemFont`
  </acceptance_criteria>
  <done>字体栈与 UI-SPEC 一致</done>
</task>

<task type="auto">
  <name>统计行与正文排版阶梯</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>定位 `.stats`、`.stats__t`（或等价类），将 **计时、步数** 的 `font-size` 设为 **≥ 26rpx**（推荐 28–30rpx），`font-weight` 500–600；次要说明用 `$ui-label-secondary`。区块标题（若有 `.section-title`）对齐 Heading 32–34rpx。</action>
  <verify>grep 验证</verify>
  <acceptance_criteria>
    - `index.scss` 中 `.stats` 相关规则含 `font-size:` 且数值 ≥ `26rpx`
    - 出现 `$ui-label-secondary` 或 `$ui-label-primary` 引用
  </acceptance_criteria>
  <done>EXP-03 字号满足</done>
</task>

<task type="auto">
  <name>模态与主按钮表面（Apple 向）</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss,src/pages/index/index.tsx</read_first>
  <action>更新 `.modal__card`：背景使用 `$ui-surface`，边框/阴影使用 `$ui-separator` 与轻 `box-shadow`（如 `0 8rpx 32rpx rgba(0,0,0,0.12)`）。`.modal__title` 颜色 `$ui-label-primary`；`.modal__body` 用 `$ui-label-primary` + 行高 1.35。**胜利弹层主按钮**（`.modal__btn` 非 ghost）：背景改为 **纯色 `$ui-accent`** 或 `linear-gradient` 仅微调明度，**禁止**保留深灰 `#292524` 作为主 CTA。`border-radius` 卡片 **24rpx**、按钮 **16rpx**（UI-SPEC spacing 表）。Ghost 按钮：白底 + `$ui-separator` 边框 + `$ui-label-primary` 字色。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.scss` 中 `.modal__btn` 规则块内 **不包含** `#292524` 作为 background 主色（grep `modal__btn` 段）
    - `.modal__card` 段含 `$ui-surface`
  </acceptance_criteria>
  <done>模态层级与 accent CTA 符合 UI-SPEC</done>
</task>

<task type="auto">
  <name>棋盘容器与工具按钮表面</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss</read_first>
  <action>将 `.grid` 外包层或棋盘衬底（若存在 `.board-wrap` / `.panel`）背景改为 `$ui-surface` 或半透明白 `rgba(255,255,255,0.92)` 变量化；宫格线颜色向 `$ui-separator` 靠拢。工具行按钮（撤销/擦除/新局）统一圆角与边框 token，避免与主 CTA 同色铺满（非 accent 按钮用 neutral surface）。</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - 含 `.grid` 的规则块中出现 `$ui-surface` 或 `$ui-separator` 之一
  </acceptance_criteria>
  <done>EXP-01 表面层级一致</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] 目视：设置/帮助/胜利弹层白底与蓝主按钮
</verification>

<success_criteria>
- 模态与统计排版符合 UI-SPEC
</success_criteria>

<output>
完成后创建 `01-02-SUMMARY.md`
</output>

## PLANNING COMPLETE
