---
phase: 01-apple-ui-motion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/theme.ts
  - src/pages/index/_ui-tokens.scss
  - src/pages/index/index.scss
autonomous: true
requirements:
  - TEC-01
  - EXP-01
user_setup: []
must_haves:
  truths:
    - theme.ts 含 export 名 `appleUi`，且含 color / motion 分组
    - index.scss 首行附近 import `_ui-tokens.scss`
    - .page 背景色引用 Sass 变量而非裸 hex（除 tokens 定义文件外）
  artifacts:
    - src/utils/theme.ts
    - src/pages/index/_ui-tokens.scss
  key_links:
    - `_ui-tokens.scss` 中主色与 `01-UI-SPEC.md` Color 表一致
---

<objective>
建立 Apple 向设计 **Token 单一事实来源**：TypeScript 常量 + Sass 变量，并将 `.page` 根背景迁移到变量，为后续样式与动效任务铺路。
</objective>

<execution_context>
@C:\Users\yuqiu\.cursor\get-shit-done\workflows\execute-plan.md
@C:\Users\yuqiu\.cursor\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-apple-ui-motion/01-UI-SPEC.md
@.planning/phases/01-apple-ui-motion/01-CONTEXT.md
@src/utils/theme.ts
@src/pages/index/index.scss
</context>

<threat_model>
**Trust boundaries:** 无新增网络或存储；仅客户端样式与常量。

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-UI-01 | Tampering | 本地样式 | Low | 不涉及安全决策；不引入第三方脚本 |
| — | Information disclosure | — | N/A | 无新数据收集 |

**Notes:** 本 Phase 为表现层；STRIDE 其余项不适用。
</threat_model>

<tasks>

<task type="auto">
  <name>扩展 theme.ts — appleUi 常量</name>
  <files>src/utils/theme.ts</files>
  <read_first>src/utils/theme.ts,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>在保留现有 `figma` 导出的前提下，新增 `export const appleUi = { color: { bg, surface, separator, labelPrimary, labelSecondary, accent, destructive, conflictTint }, motion: { durationTapMs: 160, durationFocusMs: 200, durationSheetEnterMs: 360, durationSheetExitMs: 280, easeStandard: 'cubic-bezier(0.25, 0.1, 0.25, 1)', easeSheet: 'cubic-bezier(0.32, 0.72, 0, 1)' }, radiiPxNote: 'use rpx in SCSS — card 24rpx, btn 16rpx per UI-SPEC' } as const`。color 的 hex 须与 UI-SPEC 表一致：`bg` 使用 `#F5F2EB`（纸感融合），`surface` `#FFFFFF`，`separator` `#E5E5EA`，`labelPrimary` `#1C1C1E`，`labelSecondary` `#8E8E93`，`accent` `#007AFF`，`destructive` `#FF3B30`，`conflictTint` 使用 `rgba(255, 59, 48, 0.22)`。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/utils/theme.ts 含字面量 `appleUi`
    - src/utils/theme.ts 含 `#007AFF` 与 `cubic-bezier(0.32, 0.72, 0, 1)`
    - `figma` 导出未被删除
  </acceptance_criteria>
  <done>tsc 无错误且 grep 条件满足</done>
</task>

<task type="auto">
  <name>新增 _ui-tokens.scss 并在 index.scss 引入</name>
  <files>src/pages/index/_ui-tokens.scss,src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss,.planning/phases/01-apple-ui-motion/01-UI-SPEC.md</read_first>
  <action>新建 `src/pages/index/_ui-tokens.scss`，定义 Sass 变量：`$ui-bg`, `$ui-surface`, `$ui-separator`, `$ui-label-primary`, `$ui-label-secondary`, `$ui-accent`, `$ui-destructive`, `$ui-conflict-tint`，值与 `appleUi.color` 一致；另定义 `$ui-ease-standard`, `$ui-ease-sheet`（字符串带引号），`$ui-dur-tap: 160ms` 等。在 `index.scss` **文件最顶部**（在 `.page {` 之前）新增一行：`@import './ui-tokens.scss';`（项目当前无 Sass module，避免 `@use` 与既有构建不兼容）。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - 文件 `src/pages/index/_ui-tokens.scss` 存在
    - `index.scss` 包含子串 `ui-tokens`
    - `_ui-tokens.scss` 包含 `$ui-accent`
  </acceptance_criteria>
  <done>文件存在且 index 已 import</done>
</task>

<task type="auto">
  <name>将 .page 根背景迁移到 token 变量</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss,src/pages/index/_ui-tokens.scss</read_first>
  <action>将 `.page` 的 `linear-gradient` / `radial-gradient` 中 **主底色** 替换为使用 `$ui-bg` 与 `$ui-surface` 的等价渐变（可保留结构，把 `#faf7f2`、`#f0ebe3`、`#e5dfd4` 等替换为基于 `$ui-bg` 的 `color-mix` 或两档近似色；若 `color-mix` 兼容性担忧，则至少 **最底层背景** 使用 `$ui-bg`）。不得删除 `env(safe-area-inset-*)` 的 padding 行。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `.page {` 与 `padding-bottom: calc` 仍存在于 index.scss
    - `.page` 规则块内出现 `$ui-bg` 或 `ui.$ui-bg`（取决于 @use 语法）
  </acceptance_criteria>
  <done>根背景引用 token 且 safe-area 保留</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` 退出码 0
- [ ] `npm run build:h5` 可执行且无 error（若环境有 node_modules）
</verification>

<success_criteria>
- Token 双轨建立完成
- 无删除 figma 或破坏现有导出
</success_criteria>

<output>
完成后创建 `01-01-SUMMARY.md`（由执行器）
</output>

## PLANNING COMPLETE
