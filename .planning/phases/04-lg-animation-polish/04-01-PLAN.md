---
phase: 04-lg-animation-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/_motion-tokens.scss
  - src/styles/_component-tokens.scss
  - src/styles/_mixins-lg.scss
  - src/pages/index/_ui-tokens.scss
  - src/utils/theme.ts
autonomous: true
requirements:
  - TOK-01
  - TOK-02
  - TOK-03
  - TOK-04
user_setup: []
must_haves:
  truths:
    - 三个新 SCSS 文件存在且被 `_ui-tokens.scss` 或等价入口转发
    - theme.ts 的 appleUi.motion / appleUi.glass 与 SCSS 新变量数值可逐项对照
  artifacts:
    - src/styles/_motion-tokens.scss
    - src/styles/_component-tokens.scss
    - src/styles/_mixins-lg.scss
  key_links:
    - 与 04-UI-SPEC.md 真源表一致
---

<objective>
建立 Phase 4 **Token 与 mixin 基础设施**：`$ease-spring-*`、时长族、`linear()`+回退、组件级语义变量与 `liquid-glass`/`spring-btn`/`cell-interactive` mixin，并同步 `theme.ts`。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-lg-animation-polish/04-UI-SPEC.md
@.planning/phases/04-lg-animation-polish/04-RESEARCH.md
@.planning/research/SUMMARY.md
@src/styles/_apple-ui-tokens.scss
@src/utils/theme.ts
@src/pages/index/_ui-tokens.scss
</context>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-04-01 | Tampering | 本地 token | N/A | 无外部输入；不引入新脚本 |
| — | — | — | — | 仅样式与 TS 常量 |

**Notes:** 表现层；无新网络边界。
</threat_model>

<tasks>

<task type="auto">
  <name>新增 _motion-tokens.scss</name>
  <files>src/styles/_motion-tokens.scss</files>
  <read_first>src/styles/_apple-ui-tokens.scss,.planning/REQUIREMENTS.md,.planning/phases/04-lg-animation-polish/04-UI-SPEC.md</read_first>
  <action>新建 `src/styles/_motion-tokens.scss`。至少包含：`$ease-spring-snappy` / `$ease-spring-default` / `$ease-spring-gentle` / `$ease-spring-stiff` 作为 **cubic-bezier 或 linear() 字符串**（每个变量旁注释回退名）；`$dur-micro` `$dur-fast` `$dur-default` `$dur-enter` `$dur-exit` `$dur-ambient`（与 REQ 中 120/140/180/220/240/280/300/380ms 等**对得上**的命名）；`$ease-overshoot-press`（回弹 240ms 用）。不得删除 `_apple-ui-tokens` 中已有 `$ui-ease-*` 的引用点——新 token **并行存在**，由 mixin 选新或旧。文件首行可 `@use 'sass:string'` 如需要。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - 文件 `src/styles/_motion-tokens.scss` 存在
    - 文件内出现子串 `ease-spring` 与 `dur-`
  </acceptance_criteria>
  <done>grep 与文件存在</done>
</task>

<task type="auto">
  <name>新增 _component-tokens.scss</name>
  <files>src/styles/_component-tokens.scss</files>
  <read_first>src/styles/_motion-tokens.scss,.planning/REQUIREMENTS.md</read_first>
  <action>新建 `src/styles/_component-tokens.scss`，`@use` relative `_motion-tokens.scss` 与 `_apple-ui-tokens.scss`（路径 `@use 'apple-ui-tokens' as *` 如与目录平级在 `src/styles/`）。定义：`$btn-pressed-scale` 0.95、`$btn-disabled-opacity` 0.38、`$cell-selected-tint`（用已有 `$ui-semantic-dim-2` 或引用）、`$modal-enter-duration` 380ms、`$modal-exit-duration` 280ms、`$switch-squash-scale` 0.85、`$switch-stretch-scale` 1.15；**全部引用 primitive，不出现裸 6 位色 hex**。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - 文件存在且含 `$btn-pressed-scale` 与 `$modal-enter-duration`
  </acceptance_criteria>
  <done>grep 通过</done>
</task>

<task type="auto">
  <name>新增 _mixins-lg.scss</name>
  <files>src/styles/_mixins-lg.scss</files>
  <read_first>src/styles/_component-tokens.scss,.planning/REQUIREMENTS.md</read_first>
  <action>新建 `src/styles/_mixins-lg.scss`，`@use` component + apple tokens。实现至少： `@mixin liquid-glass()`（封装 blur/saturate/brightness 组合占位，**参数化 blur px**）; `@mixin spring-btn()`（为按钮准备 transition 公共块，用 motion token）; `@mixin cell-interactive()`（为 `.cell` 准备 transition+will-change 占位）。mixin 体可为 5–20 行级可编译 SCSS，供 Wave 2/3 消费。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - 文件含 `@mixin liquid-glass` 与 `@mixin spring-btn` 与 `@mixin cell-interactive`
  </acceptance_criteria>
  <done>grep 通过</done>
</task>

<task type="auto">
  <name>转发新 token 到页面</name>
  <files>src/pages/index/_ui-tokens.scss</files>
  <read_first>src/pages/index/_ui-tokens.scss</read_first>
  <action>在 `src/pages/index/_ui-tokens.scss` 中保留对 `../../styles/apple-ui-tokens` 的 `@forward`，**追加** `@forward '../../styles/motion-tokens';` `@forward '../../styles/component-tokens';` `@forward '../../styles/mixins-lg';`（若 Sass 要求 `@forward` 顺序与已有冲突，可改为在 `_apple-ui-tokens` 末 `@use` 新件——**但若如此须在 acceptance 中说明**且保证 `index.scss` 的 `@use './ui-tokens' as *` 仍可见新变量）。`npm run build:h5` 须无 Sass 错。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - `_ui-tokens.scss` 出现子串 `motion-tokens` 与 `mixins-lg`
  </acceptance_criteria>
  <done>build:h5 成功</done>
</task>

<task type="auto">
  <name>同步 theme.ts — appleUi.motion 与 glass</name>
  <files>src/utils/theme.ts</files>
  <read_first>src/utils/theme.ts,src/styles/_motion-tokens.scss,src/styles/_component-tokens.scss</read_first>
  <action>扩展 `appleUi`：1）`motion` 下增加与 SCSS 对齐的 `spring` 子对象或平铺键（`springSnappy`/`springDefault`/`springGentle`/`springStiff` 字符串，值与对应 `$ease-spring-*` 一致）。2）`duration` 类：`enterModalMs: 380` `exitModalMs: 280` `cellRippleStaggerMs: 8` 等 REQ 有明确数字的项。3）`glass` 子对象：含 `contentBlur` 18、`modalBlur` 22、与 `MAT-01/02` 一致的 saturate/brightness 数字（**与将写入 Wave 3 的 SCSS 目标一致**）。**保留** `figma` 与既有 `color`/`board`/`hero` 键。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `theme.ts` 含字面量 `springSnappy` 或 `enterModalMs` 与 `380`
    - `theme.ts` 含 `contentBlur` 与数字 `18`
  </acceptance_criteria>
  <done>tsc 通过</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` 成功
- [ ] `npm run build:h5` 成功
</verification>

<success_criteria>
- TOK-01～04 的「文件真源 + TS 双轨」落地
- 不破坏现有 build
</success_criteria>

## PLANNING COMPLETE
