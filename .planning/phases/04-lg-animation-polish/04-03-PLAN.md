---
phase: 04-lg-animation-polish
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/app.scss
  - src/pages/index/index.tsx
  - src/pages/index/index.scss
autonomous: true
requirements:
  - MOD-01
  - MOD-02
  - MOD-03
  - MOD-04
  - MOD-05
  - WIN-01
  - WIN-02
  - WIN-03
  - SW-01
  - SW-02
  - SW-03
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
  - MAT-05
  - HERO-01
  - HERO-02
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - KBD-01
  - KBD-02
user_setup: []
must_haves:
  truths:
    - 弹层关闭有 leaving 时序，时长与 MOD-02/04 数字一致
    - WIN-02 的 rAF 在 effect cleanup 中 cancel
    - MAT-05 的类或 data 属性在 deviceMemory<4 时关闭 backdrop-filter
  artifacts:
    - `src/app.scss`（reduced motion）
  key_links:
    - 04-UI-SPEC
---

<objective>
完成 **弹层/通关/开关/材质/Hero/无障碍/键盘** 全量 iOS 26 向打磨；**`index.scss` 内修改按下方任务顺序串行**（与跨 AI 评审建议一致，避免 850+ 行文件合并冲突）。若 Taro `Switch` 无法样式化 thumb，则 **H5 用自定义 `View` 滑块** 替代以满足 SW-01~03。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-lg-animation-polish/04-UI-SPEC.md
@.planning/phases/04-lg-animation-polish/04-01-PLAN.md
@.planning/phases/04-lg-animation-polish/04-02-PLAN.md
@src/app.scss
@src/pages/index/index.tsx
@src/pages/index/index.scss
</context>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-04-03 | Information disclosure | 无新采集 | N/A | deviceMemory 仅读，不上传 |
| — | — | — | — | 客户端 UI |

**Notes:** 无新信任边界。
</threat_model>

<tasks>

<task type="auto">
  <name>（序1）弹层 data-state 与 leaving + MOD-01~05</name>
  <files>src/pages/index/index.tsx,src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss（.modal 段全段）,src/pages/index/index.tsx（三个 modal 与 wonOpen 状态机）</read_first>
  <action>1）为 **won / settings / help** 各引入独立可见态：`open` 与 `leaving`；关闭时先设 `leaving`、在 **280ms**（与 `theme.ts` exit 一致）后卸载 DOM 并复位 state。2）`modal__mask` / `modal__card` 的动画与 **REQ MOD-01~03** 数值对齐（入 380ms 等）。3）**MOD-05**：`.modal`  open 时给 `.content-wrap` 加类（如 `content-wrap--modal-dim`）将 blur 18px→8px/200ms。4）处理 **开→关 连点**（leaving 中忽略重复关闭）。**本任务只改** modal 块与 content-wrap 相关 SCSS 块，不触碰 hero/switch。 </action>
  <verify>npx tsc --noEmit; npm run build:h5</verify>
  <acceptance_criteria>
    - `index.tsx` 含子串 `leaving` 与 `setTimeout` 或等价定时卸载
    - `index.scss` 含 `content-wrap--modal` 或 `modal-dim` 与 MOD-05 语义（blur 降低）
  </acceptance_criteria>
  <done>build 通过</done>
</task>

<task type="auto">
  <name>（序2）WIN-01/02/03 通关层编排 + rAF 清理</name>
  <files>src/pages/index/index.tsx,src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.tsx（恭喜块）,src/utils/theme.ts</read_first>
  <action>1）**WIN-01** 为蒙层/卡片/标题/数字区/按钮设 **stagger**（0/40/120/200/300ms 类，与 REQ 一致，允许 ±20ms 取整到帧）。2）**WIN-02**：将用时与步数从静态 `Text` 拆出为 **自增显示**；`useRef`+`requestAnimationFrame`+easeOutQuart 400ms；在 **useEffect return** 中 `cancelAnimationFrame(rafId)`。3）**WIN-03**：确认/增强顶部 spectrum 条使用 `$ui-modal-spectrum-*`。4）**不得** 在无 won 时跑 rAF。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.tsx` 含 `cancelAnimationFrame` 子串
    - `index.tsx` 含 `requestAnimationFrame` 子串
  </acceptance_criteria>
  <done>tsc 通过</done>
</task>

<task type="auto">
  <name>（序3）Switch SW-01~03</name>
  <files>src/pages/index/index.tsx,src/pages/index/index.scss</files>
  <read_first>REQUIREMENTS SW 段</read_first>
  <action>若 **Taro Switch** 的 DOM 在 H5 上无法为 thumb 加 `transform`：实现 **`settings-toggle` 组件**（两 `View` 轨道+thumb，受控 `checked`），`onClick` 切换并调用 `patchSettings`。**实现** `translateX` + `scaleX(0.85/1.15)` 与 **track 颜色** 280ms 过渡（SW-03）。若可保留 Taro `Switch` 且验收满足 SW，须在 PR 说明中附 **Safari 截图**；否则以自定义为准。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - `index.scss` 含 `settings-toggle` 或 `switch` 与 `scaleX(0.85` 之一直串出现（或 Taro 覆盖层）
  </acceptance_criteria>
  <done>build 通过</done>
</task>

<task type="auto">
  <name>（序4）MAT-01~05 材质 + 低端回退</name>
  <files>src/pages/index/index.tsx,src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss（.content-wrap, .modal__card, ::after）</read_first>
  <action>1）**MAT-01/02** 升级 blur/saturate/brightness 与 `@supports not` 保留。2）**MAT-03** 为 `.content-wrap::after` 加 hover/pressed 的 opacity 过渡（与 REQ）。3）**MAT-04** 添加 `::before` 内阴影。4）**MAT-05**：`useEffect` 中若 `'deviceMemory' in navigator && (navigator as any).deviceMemory < 4`，给 `body` 或 `.page` 加类 **`device-memory-reduce`**，在 SCSS 中该类下 **移除此页 `backdrop-filter`**（`none` 或实色，与无 blur 回退一致）。5）`typeof navigator` 与 SSR 安全。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - `index.tsx` 或 `app.scss` 出现 `deviceMemory` 子串
    - `index.scss` 或 `app.scss` 出现 `device-memory-reduce` 子串
  </acceptance_criteria>
  <done>build 通过</done>
</task>

<task type="auto">
  <name>（序5）HERO-01/02</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss（.hero__a~d, keyframes）</read_first>
  <action>1）**HERO-01**：在 `@supports` 可用时，为 hero 加 **scroll-driven**（`animation-timeline: scroll()`）使随 ScrollView 滚动淡出；`@supports not` 时降级为**静态 opacity 1** 或不加规则。2）**HERO-02**：将现有 rpx 位移**数值×0.7**（符合 REQ「降低 30%」— 在注释中标原始值）。3）`prefers-reduced-motion` 在下一任务与全局统一。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - `index.scss` 含 `scroll` 与 `animation-timeline` 或含注释说明不支持降级
  </acceptance_criteria>
  <done>build 通过</done>
</task>

<task type="auto">
  <name>（序6）A11Y-01~03 — app.scss</name>
  <files>src/app.scss</files>
  <read_first>src/app.scss</read_first>
  <action>在 `@media (prefers-reduced-motion: reduce)` 下对 `*,*::before,*::after` 关闭 `animation`/`transition` 为主；设 **A11Y-02 白名单**（`btn` 短 opacity、modal 200ms 仅淡入 等）用**更高特异度** 选择器加回。A11Y-03：对 `.hero` 与 scroll-driven 类名**显式** `animation: none` / 关闭 timeline。与 `REQUIREMENTS` 文一致。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - `app.scss` 含 `prefers-reduced-motion` 与 `*::before`
    - `app.scss` 或 `index.scss` 含 `hero` 与 `animation` 规则
  </acceptance_criteria>
  <done>build 通过</done>
</task>

<task type="auto">
  <name>（序7）KBD-01/02 焦点与键盘</name>
  <files>src/pages/index/index.tsx,src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.tsx（onKey 与 selectCell）,.planning/REQUIREMENTS KBD 段</read_first>
  <action>1）**KBD-01** 为 `.btn`、可聚焦格、（自定义）switch 加 `:focus-visible` 环：`2px solid $ui-accent`、offset 2px、100ms。2）**KBD-02**：当用方向键改变 `selected` 时，为当前格加 **`data-keyboard-focus="true"`**（或容器单一指示），在 SCSS 画环；`Tab` 行为保持现状；在 TS **注释**说明「玩法选中与 Tab 环分离」策略。3）不破坏既有 `onKey` 逻辑。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.scss` 含 `focus-visible` 与 `ui-accent` 或 `$ui-accent`
    - `index.tsx` 含 `data-keyboard-focus` 或注释说明 KBD-02
  </acceptance_criteria>
  <done>tsc 通过</done>
</task>

<task type="auto">
  <name>（序8）终检与全量 build</name>
  <files>—</files>
  <read_first>—</read_first>
  <action>按 ROADMAP 成功准则运行 **`npx tsc --noEmit`** 与 **`npm run build:h5`**；人工清单：iOS 弹层关、振动开关、同数涟漪、低端类。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - 两条命令均退出码 0
  </acceptance_criteria>
  <done>CI 本地等价通过</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build:h5`
- [ ] 手测：Phase 4 ROADMAP 成功准则 1–4 抽样
</verification>

<success_criteria>
- 全部 MOD/WIN/SW/MAT/HERO/A11Y/KBD/MAT-05 在代码中有对应
- 无未清理 rAF
</success_criteria>

## PLANNING COMPLETE
