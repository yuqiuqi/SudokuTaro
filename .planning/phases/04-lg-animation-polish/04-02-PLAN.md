---
phase: 04-lg-animation-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/pages/index/index.tsx
  - src/pages/index/index.scss
autonomous: true
requirements:
  - BTN-01
  - BTN-02
  - BTN-03
  - BTN-04
  - CELL-01
  - CELL-02
  - CELL-03
  - CELL-04
user_setup: []
must_haves:
  truths:
    - `cell--same` 只出现在「同行/列/宫内」的同数字格；且每格有 `--cell-ripple-index`（仅 hl 为 true 时）
    - `.btn--action` 具有 data-state 与 specular 的 scaleX 动画（非 width）
  artifacts:
    - `src/pages/index/index.tsx`（SudokuGrid 与 按钮区）
  key_links:
    - 04-UI-SPEC 五态与 CELL-02
---

<objective>
在 Wave 1 token 上完成 **主操作按钮** 与 **数独格** 的 iOS 26 向动效与五态；**修正**同数高亮范围为行/列/宫，并实现 `--cell-ripple-index` 涟漪时序（≤20 格）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-lg-animation-polish/04-UI-SPEC.md
@.planning/phases/04-lg-animation-polish/04-01-PLAN.md
@src/pages/index/index.tsx
@src/pages/index/index.scss
</context>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-04-02 | XSS | 无新 HTML 插值 | N/A | 仅与既有数字与 class 有关 |

**Notes:** 客户端 UI。
</threat_model>

<tasks>

<task type="auto">
  <name>同数高亮限制为行/列/宫 + ripple 序</name>
  <files>src/pages/index/index.tsx</files>
  <read_first>src/pages/index/index.tsx（SudokuGrid 全段）</read_first>
  <action>在 `SudokuGrid` 内：1）将高亮同数字条件由纯 `sameNum` 改为 **`sameNum(r,c) && regionMask(r,c)`**（`regionMask` 已存在）。2）为每个满足条件的格计算**涟漪序**：从**选中格** (sr,sc) 起，对集合内格子按**曼哈顿距离升序**（或 BFS 层序）稳定排序，得到 `0..K-1` 的 index；3）在 `View` 上 **`style`（仅 H5 对象）** 设 `--cell-ripple-index: n`；无 hl 的格不传或设 `0`。**K 应 ≤ 27**，满足 REQ 非 81 格 storm。4）`SudokuGrid` 增加 `selected` 以计算序（已存在 props）。</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.tsx` 出现 `regionMask` 与 `sameNum` 的**与**同时用于 hl 或等价变量名
    - `index.tsx` 出现子串 `--cell-ripple-index`
  </acceptance_criteria>
  <done>grep 与 tsc</done>
</task>

<task type="auto">
  <name>SCSS：CELL-01/02/03/04</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss（.cell 与 .cell--same 段）,.planning/REQUIREMENTS.md</read_first>
  <action>更新 `.cell--same` 与选中态：1）`transition-delay: calc(var(--cell-ripple-index, 0) * 8ms)`（或兼容写法）。2）**CELL-01** 选中 `scale(0.96→1.0)` + 背景 140ms，使用 `_mixins-lg` 或新的 motion token 变量。3）**CELL-03** 冲突 shake：在 `.cell--bad` 上 keyframes 含 ±4rpx 与 220ms 曲线。4）**CELL-04** 用户填数入场 `scale(0.8→1.0)` 100ms，可用 `.cell__txt` 的 class 切换（可能需 `index.tsx` 给**新填**一格打短生命周期 class——若实现成本过高，可改为「值从 0 变非 0 的同一格」检测，执行器在 TS 增加 `lastPlaced` ref）。**禁止** 81 格同时 `will-change: transform` 常驻。 </action>
  <verify>npm run build:h5</verify>
  <acceptance_criteria>
    - `index.scss` 含 `cell-ripple-index` 或 `8ms` 与 `.cell--same` 同文件出现
  </acceptance_criteria>
  <done>build 成功</done>
</task>

<task type="auto">
  <name>按钮 data-state 与 H5 按压时序</name>
  <files>src/pages/index/index.tsx,src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.tsx（actions 区）,src/pages/index/index.scss（.actions .btn--action）</read_first>
  <action>为三个主操作 `View` 增加 **`data-state`**：idle/pressed（以及 disabled 由 `btn--disabled` 与 `data-state="disabled"` 同步）。在 **H5** 使用 `onTouchStart`+`onTouchEnd`+`onTouchCancel` 设置 `pressed`；**补充** `onMouseDown`/`onMouseUp` 以方便桌面。`hoverClass` 可保留，但与 REQ 的 spring 不冲突为优先。BTN-04：`.btn--disabled` 的 opacity 0.38 与 200ms。BTN-01：120ms 至 scale(0.95) opacity(0.88)；240ms 回弹 1.03→1。数值引用 `$btn-*` 或 `theme` 在 SCSS 中映射。 </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `index.tsx` 的 actions 区出现 `data-state` 子串
    - `index.scss` 含 `[data-state` 与 `btn--action`
  </acceptance_criteria>
  <done>tsc 通过</done>
</task>

<task type="auto">
  <name>BTN-02 specular scaleX</name>
  <files>src/pages/index/index.scss</files>
  <read_first>src/pages/index/index.scss（.btn--action ::after 若存在）</read_first>
  <action>为 `.actions .btn--action`（或 `::after` 高光层）实现 **按下时 `transform: scaleX(0.5)`**，**禁止** 对 `width` 用 transition；`transform-origin: center`；opacity 0.4→0.1 / 回弹 与 REQ 一致。使用 `prefers-reduced-motion` 时与 A11Y 后续协调整（本任务可先写与 Wave 1 token 的 duration 变量）。 </action>
  <verify>grep + build:h5</verify>
  <acceptance_criteria>
    - `index.scss` 在 `.actions` 或 `btn--action` 块含 `scaleX(0.5)` 与 `::after` 或 specular 选择器
  </acceptance_criteria>
  <done>build 通过</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run build:h5`
</verification>

<success_criteria>
- 所有 BTN-、CELL- 需求有代码锚点
- 同数涟漪只在局部区域
</success_criteria>

## PLANNING COMPLETE
